import React, {useState, useEffect} from 'react';

//it will help us retrieve the data from the url
import queryString from 'query-string';
import io from 'socket.io-client';

const Chat = () => (
		<h2>Chat</h2>
);


export default Chat;